import React, { PropTypes } from 'react'

const RecruitmentContainer = React.createClass({
  render () {
    return (
      <div>This is the recruitment page</div>
    );
  }
});

export default RecruitmentContainer;
